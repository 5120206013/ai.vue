# Vue3(Vite) + SpringBoot Jar 完整部署手册

> 架构说明
>
> - 前端：Vue3 Vite，托管于 Github + Vercel（静态页面，HTTPS）
> - 后端：SpringBoot Jar，运行在阿里云 Ubuntu ECS
> - Nginx：ECS 上做 HTTPS、SSL证书、接口反向代理、CORS跨域处理
> - 数据库：后端依靠【云枢命令行客户端】隧道访问公司内网测试数据库
> - 访问链路：浏览器 → Vercel前端页面 → https://域名/api/* → ECS(Nginx:443) → 本地8090 SpringBoot → 云枢隧道 → 公司内网数据库

## 前期准备清单

1. 阿里云 Ubuntu ECS 云服务器
2. 阿里云购买域名
3. 阿里云免费SSL证书
4. Github 存放前端Vue源码
5. Vercel 绑定Github仓库
6. 云枢Linux命令行客户端

---

## 一、域名 & ICP备案（国内ECS对外服务必须）

1. **购买域名**

阿里云控制台 → 域名 → 注册域名，购买目标域名。

2. **ICP备案**

阿里云控制台搜索「ICP备案」进入备案系统

- 填写主体个人真实信息

- 网站名称、简介，规避AI、系统等敏感词汇
- 上传证件，完成人脸核验
- 提交，等待阿里云初审 + 通信管理局审核（数个工作日）

> ⚠️ 只有备案审核通过，域名才允许解析到国内ECS公网IP对外提供服务。
> 开发阶段不想备案备选：使用vercel.json做接口代理，不解析域名到国内ECS。

3. **DNS解析（备案通过后执行）**

阿里云域名控制台 → DNS解析
新增A记录：

- 主机记录：`@`
- 记录类型：A记录
- 记录值：阿里云ECS公网IP
等待解析生效。

---

## 二、阿里云ECS配置（Ubuntu）

### 2.1 安全组放行端口（阿里云控制台）

入方向放行：

- 22：SSH远程连接
- 80：HTTP（用于重定向跳转HTTPS）
- 443：HTTPS

> ❗后端8090端口**不要对外放行**，只允许本机Nginx访问。

### 2.2 安装基础依赖

```bash
sudo apt update
# 安装Nginx
sudo apt install nginx
# 安装JDK17运行SpringBoot jar
sudo apt install openjdk-17-jdk